[project]
name = "python-template"
version = "0.1.0"
requires-python = "==3.14.*"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.15",
    "ty>=0.0.42",
]
git = [
    "pre-commit>=4.6.0",
]
